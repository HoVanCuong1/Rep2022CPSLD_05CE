//
//  Pair.cpp
//  Lesson74
//
//  Created by Branium on 12/06/2021.
//
/*
#include <iostream>
#include "Pair.hpp"
using namespace std;

template<class K, class V> Pair<K, V>::Pair() {
    // do nothing
}

template<class K, class V> Pair<K, V>::Pair(K key, V value) {
    this->key = key;
    this->value = value;
}

template<class K, class V> void Pair<K, V>::setKey(K key) {
    this->key = key;
}

template<class K, class V> void Pair<K, V>::setValue(V value) {
    this->value = value;
}

template<class K, class V> K Pair<K, V>::getKey() const {
    return key;
}

template<class K, class V> V Pair<K, V>::getValue() const {
    return value;
}

template<class K, class V> std::istream& operator >> (std::istream& is, Pair<K, V>& pair) {
    cout << "Nhap key: ";
    getline(is, pair.key);
    cout << "Nhap value: ";
    getline(is, pair.value);
}

template<class K, class V> std::ostream& operator << (std::ostream& os, const Pair<K, V>& pair) {
    cout << "Key: " << pair.key << " - Value: " << pair.value << endl;
}
*/
